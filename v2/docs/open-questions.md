# Open Questions

- Should v2 be a plain web app first, then wrapped by Capacitor, or should native constraints drive the app shell from day one?
- Should approval requests be a single unified collection with `kind`, or separate collections per request type?
- What is the cleanest migration rollback story if a migrated family opens v1 again?
