# App

Application bootstrap, routing, session startup, and shell-level wiring.

This layer may coordinate domain, state, sync, platform, and feature modules. It should not contain business rules directly.
