# Features

Feature modules combine domain rules, state actions, and UI.

A feature may render screens or flows, but should avoid direct Firebase, Capacitor, or RevenueCat calls. Use platform adapters instead.
